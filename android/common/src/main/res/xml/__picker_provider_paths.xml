<?xml version="1.0" encoding="utf-8"?>
<paths>

    <!-- Represents files in the root of the external storage area. The root path of this
    subdirectory is the same as the value returned by Environment.getExternalStorageDirectory(). -->
    <external-path
        name="external_storage_root"
        path="." />

    <!-- Represents files in the files/ subdirectory of your app's internal storage area.
    This subdirectory is the same as the value returned by Context.getFilesDir(). -->
    <files-path
        name="files"
        path="." />
</paths>