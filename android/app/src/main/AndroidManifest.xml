<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.photor"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="26" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front.autofocus"
        android:required="false" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <!-- 主启动界面 -->
        <activity
            android:name=".base.MainActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:hardwareAccelerated="true"
            android:screenOrientation="portrait"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name="com.facebook.react.devsupport.DevSettingsActivity" />
        <!-- opencv 测试界面 -->
        <activity android:name=".base.activity.test.OpencvTestActivity" />

        <!-- 星野图片对齐界面 -->
        <activity android:name=".staralign.StarAlignBaseActivity" />
        <activity android:name=".base.activity.PhotoOperateResultActivity" />
        <activity android:name=".staralign.StarAlignSplitActivity" />
        <activity android:name=".staralign.GrabCutActivity" />

        <!-- 景深计算器界面 -->
        <activity android:launchMode="singleTask" android:name=".dofcalculator.activity.DofCalcMainActivity" />
        <activity android:launchMode="singleTask" android:name=".dofcalculator.activity.SensorSizeActivity" />
        <activity android:launchMode="singleTask" android:name=".dofcalculator.activity.CustomCircleOfConfusionActivity" />

        <!-- 曝光合成界面 -->
        <activity android:name=".exposure.ExposureBaseActivity" />

        <!-- 相机界面 -->
        <activity
            android:name=".camera.activity.PicturePreviewActivity"/>
        <activity
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait"
            android:name=".camera.activity.VideoPreviewActivity" />

        <!-- 公共组件，图片选择器界面 -->
        <activity android:name="com.example.photopicker.PhotoPickerActivity" />
        <activity android:name="com.example.photopicker.PhotoPagerActivity" />
    </application>

</manifest>